[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gnome-themer"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["gnome_themer*"]

[project.scripts]
gnome-themer = "gnome_themer.main:main"
